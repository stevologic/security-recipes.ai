<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="description" content="{{ (description or ('This page has moved permanently to ' + redirectTo)) | escape }}">
<meta name="robots" content="noindex,follow">
<meta http-equiv="refresh" content="0;url={{ redirectTo }}">
<link rel="canonical" href="{{ redirectTo }}">
<title>{{ title }} – {{ site.title }}</title>
</head>
<body>
<main>
  <h1>{{ title }}</h1>
  <p>This page has moved permanently. <a href="{{ redirectTo }}">Continue to the canonical page</a>.</p>
</main>
<script>location.replace("{{ redirectTo }}" + location.search + location.hash);</script>
</body>
</html>
