{# The landing page is a fully self-contained HTML document (ported from
   layouts/index.html in the Hugo build, with menu/baseURL values resolved
   statically). content/_index.md exists only to anchor the home URL. #}
{%- include "layouts/home-static.html" -%}
